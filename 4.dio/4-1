<?php
$cars = array("Audi", "BMW", "Renault", "Citroen");
$odabranoVozilo = null;

if ($_SERVER["REQUEST_METHOD"] == "POST" && isset($_POST["vozilo"])) {
    $odabranoVozilo = $_POST["vozilo"];
}
?>
<!DOCTYPE html>
<html lang="hr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista vozila — vježba 4</title>
    <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .wrapper {
            display: flex;
            gap: 24px;
            width: 100%;
            max-width: 820px;
            flex-wrap: wrap;
            justify-content: center;
        }

        /* ── Kartica ── */
        .kartica {
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            overflow: hidden;
            flex: 1;
            min-width: 280px;
            max-width: 380px;
        }

        .zaglavlje {
            background: linear-gradient(135deg, #0f3460, #533483);
            color: #fff;
            padding: 22px 28px;
        }

        .zaglavlje h2 {
            font-size: 17px;
            font-weight: 700;
            margin-bottom: 3px;
        }

        .zaglavlje p {
            font-size: 12px;
            opacity: 0.75;
        }

        .tijelo {
            padding: 24px 28px;
        }

        /* ── Lista vozila (foreach ispis) ── */
        .lista-vozila {
            list-style: none;
            margin-bottom: 0;
        }

        .lista-vozila li {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 0;
            border-bottom: 1px solid #f0f0f0;
            font-size: 15px;
            color: #333;
            font-weight: 500;
        }

        .lista-vozila li:last-child {
            border-bottom: none;
        }

        .ikona-auto {
            font-size: 20px;
        }

        /* ── Forma s radio gumbima ── */
        .forma-izbor {
            display: flex;
            flex-direction: column;
            gap: 10px;
            margin-bottom: 20px;
        }

        .radio-redak {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 14px;
            border: 2px solid #e8e8e8;
            border-radius: 10px;
            cursor: pointer;
            transition: all .2s;
        }

        .radio-redak:hover {
            border-color: #533483;
            background: #f8f5ff;
        }

        .radio-redak input[type="radio"] {
            accent-color: #533483;
            width: 18px;
            height: 18px;
            cursor: pointer;
        }

        .radio-redak.odabran {
            border-color: #533483;
            background: #f0ebff;
        }

        .radio-label {
            font-size: 15px;
            font-weight: 600;
            color: #333;
            cursor: pointer;
        }

        /* ── Gumb ── */
        .btn {
            width: 100%;
            padding: 13px;
            font-size: 15px;
            font-weight: 700;
            color: #fff;
            background: linear-gradient(135deg, #0f3460, #533483);
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: opacity .2s, transform .1s;
        }

        .btn:hover  { opacity: .88; }
        .btn:active { transform: scale(.98); }

        /* ── Rezultat odabira ── */
        .rezultat {
            margin-top: 20px;
            background: linear-gradient(135deg, #0f3460, #533483);
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            color: #fff;
            animation: fadeIn .4s ease;
        }

        .rezultat .naslov-r {
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 1px;
            opacity: 0.75;
            margin-bottom: 8px;
        }

        .rezultat .ime-vozila {
            font-size: 28px;
            font-weight: 800;
            letter-spacing: 1px;
        }

        .rezultat .emoji {
            font-size: 36px;
            margin-bottom: 6px;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to   { opacity: 1; transform: translateY(0); }
        }

        /* ── Footer ── */
        .footer {
            text-align: center;
            padding: 12px;
            font-size: 11px;
            color: #bbb;
            border-top: 1px solid #f0f0f0;
        }
    </style>
</head>
<body>

<div class="wrapper">

    <!-- ── KARTICA 1: Foreach ispis liste ── -->
    <div class="kartica">
        <div class="zaglavlje">
            <h2>&#128663; Lista vozila</h2>
            <p>Ispis pomoću foreach petlje</p>
        </div>
        <div class="tijelo">
            <?php
                echo "<ul class='lista-vozila'>";
                foreach ($cars as $car) {
                    echo "<li><span class='ikona-auto'>&#128663;</span> $car</li>";
                }
                echo "</ul>";
            ?>
        </div>
        <div class="footer">foreach petlja — vjezba4.php</div>
    </div>

    <!-- ── KARTICA 2: Odabir vozila ── -->
    <div class="kartica">
        <div class="zaglavlje">
            <h2>&#128269; Odaberi vozilo</h2>
            <p>Označi jedno vozilo i potvrdi odabir</p>
        </div>
        <div class="tijelo">

            <form method="POST" action="">
                <div class="forma-izbor">
                    <?php foreach ($cars as $car): ?>
                        <label class="radio-redak <?php echo ($odabranoVozilo === $car) ? 'odabran' : ''; ?>">
                            <input type="radio" name="vozilo" value="<?php echo $car; ?>"
                                <?php echo ($odabranoVozilo === $car) ? 'checked' : ''; ?>>
                            <span class="radio-label">&#128663; <?php echo $car; ?></span>
                        </label>
                    <?php endforeach; ?>
                </div>
                <button type="submit" class="btn">Potvrdi odabir &#8594;</button>
            </form>

            <?php if ($odabranoVozilo !== null): ?>
                <div class="rezultat">
                    <div class="emoji">&#9989;</div>
                    <div class="naslov-r">Odabrano vozilo</div>
                    <div class="ime-vozila"><?php echo htmlspecialchars($odabranoVozilo); ?></div>
                </div>
            <?php endif; ?>

        </div>
        <div class="footer">foreach + radio — vjezba4.php</div>
    </div>

</div><!-- /wrapper -->

</body>
</html>
<!-- vjezba4.php -->
